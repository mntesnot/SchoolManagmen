@model IEnumerable<Student>
@{
}

<form asp-action="Search" method="get">
	<input name="name" placeholder="Search by Name"
		class="form-control" />
	<button type="submit" >Search</button>
</form>
<table class="table">
	<thead>
		<tr>
			<th>@Html.DisplayNameFor(model => model.Id)</th>
			<th>@Html.DisplayNameFor(model => model.Name)</th>
			<th>@Html.DisplayNameFor(model => model.Age)</th>
			<th>@Html.DisplayNameFor(model => model.PhoneNo)</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
		@if (Model != null && Model.Any())
		{
			@foreach (var s in Model)
			{
				<tr>
					<td>@s.Id</td>
					<td>@s.Name</td>
					<td>@s.Age</td>
					<td>@s.PhoneNo</td>
					<td><a asp-action="Detail" asp-route-id="@s.Id">Detail</a></td>
				</tr>
			}
		}
		else
		{
			<tr>
				<td>No Data !</td>
			</tr>
		}

	</tbody>
</table>
@* @if(Model != null){
	<div>
		<ul>
			<li>Id : @Model.Id</li>
			<li>Name : @Model.Name</li>
			<li>Age : @Model.Age</li>
			<li>Phone No : @Model.PhoneNo</li>
		</ul>
	</div>
}else{
	<h1 class="bg-danger text-light">Not Found</h1>
} *@